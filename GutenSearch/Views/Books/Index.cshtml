@{
    ViewData["Title"] = "Book Page";
}

<h1>@ViewData["Title"]</h1>

@if (@Model.Count == 0)
{
	<h3>There's nothing here</h3>
}

<ul>
    @foreach (Book book in Model)
    {
			<li>
                @book.Title
                <a href="#" data-id="@book.BookId" class="delete-link emoji">❌</a>
            </li>
    }
</ul>